# Event-Reservation-Platform
The goal of this project is to develop an event reservation platform composed of two main components: an Event-reservation Server (ES) and a User Application (User). The system is designed to support multiple users operating simultaneously on different machines connected through the Internet.
